# Compiled files
build/
.dart_tool/
.packages
.pub-cache/
.pub/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Local env / secrets (never commit these)
.env
.env.local
*.local

# Flutter build output
build/
# NOTE: app/build/web/ is intentionally tracked (committed by CI for Railway to serve)

# Flutter plugins (machine-specific)
.flutter-plugins
.flutter-plugins-dependencies
.packages

# Railway / deployment artifacts
.railway/
