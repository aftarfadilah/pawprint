# Compiled files
build/
.dart_tool/
.packages
.pub-cache/
.pub/
.flutter-plugins
.flutter-plugins-dependencies

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Local env / secrets (never commit these)
.env
.env.local
*.local

# Flutter build output
build/
app/build/

# Published web build (committed to repo for Railway to serve)
# This is intentional — do NOT ignore app/build/web/

# Flutter plugins (machine-specific)
.flutter-plugins
.flutter-plugins-dependencies
.packages

# Railway / deployment artifacts
.railway/
